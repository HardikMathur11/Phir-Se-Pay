# Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173
JWT_SECRET=phir_se_pay_jwt_secret_key_demo_2026

# Razorpay Test Mode Credentials (Optional - Falls back to Simulator Mode if missing)
RAZORPAY_KEY_ID=rzp_test_samplekey123
RAZORPAY_KEY_SECRET=sample_secret_key_123
RAZORPAY_WEBHOOK_SECRET=sample_webhook_secret_123

# Gmail Integration (Optional - Falls back to Demo Inbox if missing)
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
GMAIL_SENDER=noreply@phirsepay.demo

# AI Agent Credentials (Optional - Falls back to Deterministic Mock Agent if missing)
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini

# Database & Cache (Optional - Falls back to Thread-Safe In-Memory Repository if missing)
MONGODB_URI=
REDIS_URL=
